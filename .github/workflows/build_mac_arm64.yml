name: 打包 macOS 应用 (Intel 兼容版 - 手动编译 Python 终极修复)

on:
  workflow_dispatch:

env:
  # 定义全局变量，方便后续步骤使用
  PYTHON_VERSION: "3.9.25"
  GETTEXT_PATH: "/usr/local/opt/gettext"

jobs:
  build:
    runs-on: macos-14
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    # ==========================================
    # 1. 安装依赖库 (gettext)
    # ==========================================
    - name: 安装 Homebrew 依赖
      run: |
        brew install gettext
        # 强制链接，确保库文件在标准路径下
        brew link gettext --force
        echo "✅ Gettext 已安装并链接"

    # ==========================================
    # 2. 【核心修复】手动编译安装 Python
    # 替代 actions/setup-python，彻底解决动态库加载问题
    # ==========================================
    - name: 编译并安装 Python (源码构建)
      run: |
        # 1. 下载 Python 源码
        echo "📥 下载 Python ${{ env.PYTHON_VERSION }} 源码..."
        curl -O https://www.python.org/ftp/python/${{ env.PYTHON_VERSION }}/Python-${{ env.PYTHON_VERSION }}.tgz
        tar -xzf Python-${{ env.PYTHON_VERSION }}.tgz
        cd Python-${{ env.PYTHON_VERSION }}

        # 2. 配置编译选项
        # --prefix: 安装路径
        # LDFLAGS/CPPFLAGS: 告诉编译器去哪里找 gettext 库
        echo "⚙️ 配置编译环境..."
        export LDFLAGS="-L${{ env.GETTEXT_PATH }}/lib"
        export CPPFLAGS="-I${{ env.GETTEXT_PATH }}/include"
        
        ./configure --prefix=/usr/local --enable-optimizations --with-ensurepip=install

        # 3. 编译并安装
        echo "🔨 开始编译 (这可能需要几分钟)..."
        make -j 4
        sudo make altinstall # 使用 altinstall 防止覆盖系统默认 python3
        
        # 4. 验证安装
        echo "✅ 验证 Python 版本..."
        /usr/local/bin/python3.9 --version
        
        # 5. 创建软链接方便使用
        sudo ln -sf /usr/local/bin/python3.9 /usr/local/bin/python
        sudo ln -sf /usr/local/bin/pip3.9 /usr/local/bin/pip

    # ==========================================
    # 3. 配置环境变量
    # ==========================================
    - name: 设置 Python 环境变量
      run: |
        echo "/usr/local/bin" >> $GITHUB_PATH
        echo "✅ Python 路径已添加到环境变量"

    # ==========================================
    # 4. 安装项目依赖
    # ==========================================
    - name: 安装依赖 (预装兼容版 Numpy)
      run: |
        pip --version
        pip install --upgrade pip
        
        # 强制安装兼容版 Numpy
        echo "📦 安装 Numpy..."
        pip install "numpy<1.22"
        
        # 安装其他依赖
        if [ -f requirements.txt ]; then
          pip install -r requirements.txt
        fi
        
        # 安装打包工具
        pip install nuitka

    # ==========================================
    # 5. 执行打包
    # ==========================================
    - name: 执行 Nuitka 打包
      run: |
        python -m nuitka \
          --standalone \
          --macos-create-app-bundle \
          --macos-app-name="商委订单审核助手服务端 (Intel版)" \
          --macos-app-icon=none \
          --jobs=2 \
          --output-dir=dist_nuitka \
          --remove-output \
          main.py

    - name: 上传 Nuitka 产物
      uses: actions/upload-artifact@v4
      with:
        name: macOS-Intel-App
        path: dist_nuitka/商委订单审核助手服务端\ \(Intel版\).app

  # ==========================================
  # 测试环节
  # ==========================================
  test-on-clean-macos:
    name: 🧪 验证 Web 服务启动
    runs-on: macos-14
    needs: build
    steps:
    - name: 下载刚才打包好的 .app
      uses: actions/download-artifact@v4
      with:
        name: macOS-Intel-App
        path: ./downloaded-app

    - name: 启动服务并实时监控日志
      run: |
        APP_PATH=$(find ./downloaded-app -name "*.app" | head -n 1)
        EXEC_PATH="$APP_PATH/Contents/MacOS/main"
        chmod +x "$EXEC_PATH"
        "$EXEC_PATH" > server_output.log 2>&1 &
        
        MAX_WAIT=60
        WAIT_UNTIL=$(($(date +%s) + MAX_WAIT))
        
        while true; do
            if [ $(date +%s) -gt $WAIT_UNTIL ]; then
                echo "❌ 等待超时"
                cat server_output.log
                exit 1
            fi
            if grep -q "Uvicorn running on http://127.0.0.1:5678" server_output.log; then
                echo "✅ 启动成功"
                break
            fi
            sleep 2
        done

    - name: 最终连通性测试
      run: |
        HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" --max-time 5 http://127.0.0.1:5678/ || echo "000")
        if [ "$HTTP_CODE" == "200" ] || [ "$HTTP_CODE" == "404" ]; then
            echo "✅ 接口响应正常 (HTTP $HTTP_CODE)"
        else
            echo "❌ 接口异常 (HTTP $HTTP_CODE)"
            exit 1
        fi
