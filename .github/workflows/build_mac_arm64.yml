name: 打包 macOS 应用 (Conda 强制 x86_64 模式)

on:
  workflow_dispatch:

env:
  CONDA_ENV_NAME: "x86_env"
  PYTHON_VERSION: "3.9"
  CONDA_SUBDIR: "osx-64"

jobs:
  build:
    runs-on: macos-14
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 安装 Miniconda (x86_64)
      run: |
        echo "📥 下载 x86_64 版 Miniconda..."
        curl -o miniconda.sh https://repo.anaconda.com/miniconda/Miniconda3-latest-MacOSX-x86_64.sh
        bash miniconda.sh -b -p $HOME/miniconda
        source $HOME/miniconda/etc/profile.d/conda.sh
        conda config --set subdir osx-64
        conda create -n ${{ env.CONDA_ENV_NAME }} python=${{ env.PYTHON_VERSION }} -c conda-forge -y
        echo "✅ x86_64 Conda 环境创建完成"

    - name: 安装项目依赖
      run: |
        source $HOME/miniconda/etc/profile.d/conda.sh
        conda activate ${{ env.CONDA_ENV_NAME }}
        
        echo "🔍 验证 Python 架构..."
        python -c "import platform; print(platform.machine()); assert platform.machine() == 'x86_64', '架构错误！'"
        
        pip install --upgrade pip
        pip install "numpy<1.22"
        pip install torch torchvision torchaudio
        pip install paddlepaddle
        pip install nuitka
        
        if [ -f requirements.txt ]; then
          pip install -r requirements.txt
        fi

    - name: 执行 Nuitka 打包
      run: |
        source $HOME/miniconda/etc/profile.d/conda.sh
        conda activate ${{ env.CONDA_ENV_NAME }}
        
        echo "📦 开始 Nuitka 打包..."
        
        # 【关键修复】将所有参数写在一行，避免 YAML 换行符导致的解析错误
        python -m nuitka \
          --standalone \
          --macos-create-app-bundle \
          --macos-app-name="商委订单审核助手服务端 (Intel版)" \
          --macos-app-icon=none \
          --jobs=2 \
          --output-dir=dist_nuitka \
          --remove-output \
          --no-pyi-file \
          --noinclude-unittest-mode=allow \
          --nofollow-import-to=torch \
          --nofollow-import-to=numpy \
          --nofollow-import-to=PIL \
          --nofollow-import-to=paddle \
          main.py

    - name: 手动复制依赖到包内
      run: |
        source $HOME/miniconda/etc/profile.d/conda.sh
        conda activate ${{ env.CONDA_ENV_NAME }}
        
        echo "🔧 正在手动注入依赖库..."
        
        APP_PATH=$(find dist_nuitka -name "*.app" | head -n 1)
        LIB_PATH="$APP_PATH/Contents/MacOS"
        
        SITE_PACKAGES=$(python -c "import site; print(site.getsitepackages()[0])")
        
        for pkg in torch numpy paddle torchvision; do
          if [ -d "$SITE_PACKAGES/$pkg" ]; then
            echo "📦 复制 $pkg..."
            cp -r "$SITE_PACKAGES/$pkg" "$LIB_PATH/"
          fi
        done
        
        if [ -d "$SITE_PACKAGES/PIL" ]; then
            cp -r "$SITE_PACKAGES/PIL" "$LIB_PATH/"
        fi

        echo "✅ 库注入完成"

    - name: 上传 Nuitka 产物
      uses: actions/upload-artifact@v4
      with:
        name: macOS-Intel-App
        path: dist_nuitka/商委订单审核助手服务端\ $Intel版$.app

  # ==========================================
  # 二阶段：测试环节
  # ==========================================
  test-on-clean-macos:
    name: 🧪 验证 Web 服务启动
    runs-on: macos-14
    needs: build
    steps:
    - name: 下载刚才打包好的 .app
      uses: actions/download-artifact@v4
      with:
        name: macOS-Intel-App
        path: ./downloaded-app

    - name: 启动服务并实时监控日志
      run: |
        APP_PATH=$(find ./downloaded-app -name "*.app" | head -n 1)
        EXEC_PATH="$APP_PATH/Contents/MacOS/main"
        chmod +x "$EXEC_PATH"
        
        # 在 M1 上运行 Intel 程序，使用 Rosetta 转译
        arch -x86_64 "$EXEC_PATH" > server_output.log 2>&1 &
        
        echo "⏳ 等待服务启动..."
        MAX_WAIT=60
        WAIT_UNTIL=$(($(date +%s) + MAX_WAIT))
        
        while true; do
            if [ $(date +%s) -gt $WAIT_UNTIL ]; then
                echo "❌ 等待超时，服务未启动"
                cat server_output.log
                exit 1
            fi
            if grep -q "Uvicorn running on http://127.0.0.1:5678" server_output.log; then
                echo "✅ 服务启动成功"
                break
            fi
            sleep 2
        done

    - name: 最终连通性测试
      run: |
        echo "🔍 测试接口连通性..."
        HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" --max-time 5 http://127.0.0.1:5678/ || echo "000")
        if [ "$HTTP_CODE" == "200" ] || [ "$HTTP_CODE" == "404" ]; then
            echo "✅ 接口响应正常 (HTTP $HTTP_CODE)"
        else
            echo "❌ 接口异常 (HTTP $HTTP_CODE)"
            cat server_output.log
            exit 1
        fi
